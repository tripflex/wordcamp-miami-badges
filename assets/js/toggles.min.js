jQuery(function($){
	$('body').on('click', '.wordcamp_miami_badges-toggle-group-buttons .button', function(){
		var clicked = $(this),
			parent = clicked.parent(),
			input = parent.next();

		parent.find('.button').removeClass('button-primary');
		clicked.addClass('button-primary');
		input.val(clicked.data('value'));
	});
});
